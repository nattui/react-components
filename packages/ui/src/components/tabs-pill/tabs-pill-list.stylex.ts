import { create } from "@stylexjs/stylex"

export const tabsPillListStyles = create({
  base: {
    columnGap: 8,
    display: "flex",
    isolation: "isolate",
    overflowX: "auto",
    position: "relative",
  },
})
