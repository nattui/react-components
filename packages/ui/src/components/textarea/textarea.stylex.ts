import * as stylex from "@stylexjs/stylex"

export const textareaStyles = stylex.create({
  base: {
    height: "auto",
    maxHeight: "none",
    minHeight: 66,
    padding: 12,
    resize: "vertical",
  },
})
