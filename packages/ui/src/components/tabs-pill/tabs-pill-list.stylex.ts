import * as stylex from "@stylexjs/stylex"

export const tabsPillListStyles = stylex.create({
  base: {
    columnGap: 8,
    display: "flex",
    isolation: "isolate",
    overflowX: "auto",
    position: "relative",
  },
})
