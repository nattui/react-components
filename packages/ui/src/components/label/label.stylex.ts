import { create } from "@stylexjs/stylex"

export const labelStyles = create({
  base: {
    color: "var(--color-text-secondary)",
    fontSize: 13,
    fontWeight: 450,
    width: "fit-content",
  },
})
