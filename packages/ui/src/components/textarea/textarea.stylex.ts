import { create } from "@stylexjs/stylex"

export const textareaStyles = create({
  base: {
    height: "auto",
    maxHeight: "none",
    minHeight: 66,
    padding: 12,
    resize: "vertical",
  },
})
